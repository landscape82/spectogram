{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spectogram.serviceAccountName" . }}
  labels:
    {{- include "spectogram.labels" . | nindent 4 }}
{{- end }}
