apiVersion: v1
kind: Service
metadata:
  name: {{ include "spectogram.fullname" . }}
  labels:
    {{- include "spectogram.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spectogram.selectorLabels" . | nindent 4 }}
